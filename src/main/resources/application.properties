#Nombre de la aplicación
spring.application.name=ProyectoNaval

#base de datos
# spring.datasource.url=jdbc:postgresql://localhost:5432/vessel
# spring.datasource.username=
# spring.datasource.password=
# spring.datasource.driverClassName=org.postgresql.Driver
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.show-sql=true
# spring.jpa.hibernate.ddlAuto=create

# Puerto del servidor
server.port=8080
server.servlet.context-path=/api

spring.web.cors.allowed-origins=${CORS_ORIGINS:https://proyecto-naval-frontend.vercel.app/, http://localhost:3000}
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
# Configuración de JSON
spring.jackson.property-naming-strategy=SNAKE_CASE
spring.jackson.default-property-inclusion=NON_NULL

# Configuración de logs
logging.level.com.ProyectoNaval=DEBUG
logging.level.org.springframework.web=DEBUG

# Actuator endpoints (para health check)
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
